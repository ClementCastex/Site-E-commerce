{% extends 'base.html.twig' %}

{% block title %}Ajouter un produit{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/admin-add-product-styles.css') }}">
{% endblock %}

{% block body %}
    <a href="{{ path('admin_dashboard') }}" class="admin-back-btn">Retour au tableau de bord</a>
    <h1 class="admin-add-product-title">Ajouter un produit</h1>

    <div class="admin-form-container">
        {{ form_start(form) }}
            {{ form_widget(form, { 'attr': {'class': 'form-control'} }) }}
            <button type="submit" class="btn admin-form-submit">Enregistrer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
